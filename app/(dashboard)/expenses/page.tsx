import { ComingSoon } from "@/components/layout/ComingSoon";

export default function ExpensesPage() {
  return (
    <ComingSoon
      title="Expenses"
      note="Expense entry, receipt upload, and the AI categorization review queue. Next: port the expense ledger + Claude-assisted categorization."
    />
  );
}
